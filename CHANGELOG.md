# 更新日志

本文件记录每个版本的用户可见变更。发布前必须为新版本添加 `## vX.Y.Z` 小节——
release 工作流会提取对应小节作为 GitHub Release 的说明（缺失则构建失败），
应用内的更新弹窗和「更新完成」日志展示的也是这段内容。

## v2.0.3 - 2026-06-23

### 测试发布
- 此版本仅用于验证 v2.0.2 修复后的自动更新链路，包括旧进程退出、安装包校验、静默安装、自动重启与更新日志展示
- 应用功能与 v2.0.2 保持一致，不包含额外功能变更

## v2.0.2 - 2026-06-23

### 修复
- 修复下载完成后旧程序可能因 Qt 后台工作线程迟迟不退出，导致更新助手等待超时并中止安装的问题
- 更新时会先保存状态、隐藏界面并停止后台定时任务；若进程仍未退出，助手会核验 PID 对应的可执行文件路径，仅对确认属于当前应用的旧进程执行强制收尾，然后继续静默安装与重启
- 更新日志新增安装包校验结果、旧进程退出方式、安装器返回码和重启进程号，便于准确排查失败环节

### 质量保障
- 单元测试改用隔离日志，不再把模拟的安装失败记录写入真实 `%TEMP%\LiquidMemoWidget-update.log`
- 新增真实 Windows 子进程测试，覆盖路径核验、超时终止和安装继续执行流程

## v2.0.1 - 2026-06-23

### 测试发布
- 此版本专用于验证应用内自动更新的完整链路：发现新版本、下载安装包、SHA-256 校验、静默安装、自动重启与更新日志展示
- 应用功能与 v2.0.0 保持一致，不包含额外功能变更

## v2.0.0 - 2026-06-23

### 重大变更
- 移除实时 D3D11「液态玻璃」截屏折射皮肤及其 GPU 引擎，默认改为 Windows DWM 亚克力磨砂表面；显著减小发布体积与持续 GPU 开销，并避免截屏链路造成的黑屏、闪烁和显示器恢复问题
- 旧版本保存的液态玻璃设置会自动迁移到磨砂玻璃；自定义图片背景皮肤继续保留并可正常使用

### 新功能
- 新增三种窗口显示模式：普通悬浮窗口、贴边滑动隐藏和动画悬浮图标，可在设置中即时切换
- 悬浮图标支持自由拖动、位置记忆、待机浮动与高光动画；点击后在图标旁展开备忘录，鼠标离开后自动收回，并根据屏幕可用空间智能选择展开方向
- 悬浮图标右键可直接打开与系统托盘一致的设置、历史记录、显示/隐藏和退出菜单；右上角状态点会提示逾期或临期事项
- 临期高亮天数可在设置中调整（1–30 天），同时作用于待办截止时间和订阅日程
- 待办支持直接拖动排序；折叠状态可拖动窗口底边调整高度，双击底边恢复自动适应内容

### 视觉与交互
- 设置、历史记录、更新提示和更新日志窗口整体放大，标题、正文、表单控件、按钮和说明文字同步增大
- 优化悬浮模式的面板定位、淡入淡出和收回逻辑；编辑弹窗、拖动排序或调整窗口大小时不会误收回
- 优化长标题、地点、截止时间与日程时间列的宽度计算，并为滚动条预留空间，避免右侧内容被截断

### 更新与发布
- 自动更新支持开关与 12 小时检查节流，并记住用户暂缓的版本，减少重复提示和 GitHub API 请求
- 安装包与便携版新增 SHA-256 校验文件；应用安装更新前会校验下载内容，便携版则安全地跳转到发布页手动更新
- 改进开机自启路径迁移、安装程序清理和便携版打包流程；发布前自动运行完整回归测试

### 质量保障
- 状态格式升级至 v6，兼容迁移旧版贴边隐藏配置并独立保存悬浮图标位置
- 测试覆盖窗口模式迁移、跨屏定位、状态点、拖拽交互、窗口缩放、启动项、更新器与加密内容加载等关键流程

## v1.1.0 - 2026-06-16

### 新功能
- 自定义图片背景皮肤：可上传本地图片作为备忘录背景。导入图片后会弹出裁切窗口，用鼠标拖动调整位置、滚动滚轮缩放，固定不动的白色矩形框内即最终背景区域；命名后保存为皮肤，可在「设置 → 外观 → 皮肤」中随时选择
- 图片皮肤管理：在「设置 → 外观 → 图片皮肤」中可预览、添加和删除已保存的图片皮肤（内置的磨砂玻璃 / 液态玻璃为系统皮肤，不可删除）
- 图片背景随窗口大小自动填充裁切（保持比例、不变形），并根据图片整体明暗自动选择深色或浅色文字，保证待办内容清晰可读

### 改进
- 放大「发现新版本」更新提示与「更新完成」日志窗口，并整体调大标题、说明、更新内容正文与按钮的字号，更新信息看得更清楚

### 修复
- 修复裁切图片皮肤的弹窗中「皮肤名称」一行与上方图片重叠的问题

## v1.0.6 - 2026-06-16

### 新功能
- 系统级提醒：在「设置 → 提醒」中开启后，日程或带截止时间的待办临近时，会在屏幕右下角弹出 Windows 系统通知（点击通知可唤回备忘录窗口）
- 可自定义提前提醒时间（1–1440 分钟，默认 15 分钟）；全天日程固定在当天上午 9 点提醒一次
- 已提醒的事项不会重复弹窗，重启应用后也不会再次提醒
- 事项支持记录地点：添加或编辑待办时可填写地点，待办与日程会在标题下方以「📍 地点」灰色小字显示（日程的地点自动取自 ICS 订阅源的 LOCATION 字段）
- 待办行新增「✎ 编辑」按钮，可一次性修改事项的内容、地点与截止时间（点击截止时间列也会打开同一个编辑弹窗）

### 修复
- 修复日程/待办较长（换行）时，列表最上和最下一行贴着窗口边缘、看起来显示不全的问题：为列表增加上下留白并计入窗口自适应高度，同时按文字标签实际换行高度计算行高，确保每一行都完整显示

## v1.0.5 - 2026-06-14

### 修复
- 修复鼠标悬停待办 / 日程事件时提示框看不清的问题：原生提示框会继承该行的文字颜色，导致磨砂皮肤下「黑底黑字」、液态玻璃皮肤下顶部出现一层白色遮罩。现改用浅底深字的气泡提示，两种皮肤下都清晰可读
- 加固应用内自动更新：更新助手改为独立的 Python 进程（不再依赖 PowerShell）；当应用未能在超时内退出时，不再覆盖安装到运行中的程序、也不会重复启动两个实例；更新若未成功，下次启动会明确提示「上次更新失败」
- 系统托盘菜单改为从光标位置向左上方弹出，并限制在屏幕可视区域内，不再被屏幕底边裁切

### 改进
- 设置窗口进一步放大（1120×860），分类导航、设置项标题、控件与说明文字的字号整体调大，更易读
- 「添加事项」与「设置截止时间」弹窗放大、输入框字号调大，输入更舒适

### 其他
- 代码重构与质量保障：拆分 app.py 为多个 UI 模块，更新器逻辑改为无 Qt 依赖且可单元测试；新增测试套件并在发布前作为 CI 门禁运行，降低回归风险（对用户无可见变化）

## v1.0.4 - 2026-06-12

### 新功能
- 多日历订阅：可添加多个 ICS / webcal 链接，每个日历前有勾选框，勾选的日历的日程才在备忘录窗口中展示
- 订阅链接自动存档：删除的链接保留在「恢复已删除」菜单中，可一键找回；日历名称自动从订阅源读取
- 设置窗口支持按住空白处拖动

### 修复
- 「恢复默认」不再清除日历订阅和行为设置：现仅恢复「外观」分类的默认值（按钮已改名为「恢复默认外观」）
- 多个订阅中某一个同步失败时，不再影响其他日历的日程展示

## v1.0.3 - 2026-06-12

### 修复
- 修复应用内自动更新的竞态问题：旧逻辑固定等 2 秒就启动安装器，应用退出稍慢时会被安装器强行关闭并弹出报错。现在更新助手会等待应用进程真正退出后再静默安装，抑制安装器报错弹窗，且无论安装成功与否都会自动重启应用；安装日志写入 `%TEMP%\LiquidMemoWidget-update.log` 便于排查

### 改进
- 设置窗口改为左右两栏布局：左侧分类导航（外观 / 行为 / 日历订阅 / 关于），右侧显示对应分类的设置内容
- 设置项的说明文字收纳到标题旁的 ⓘ 图标中，鼠标悬停即弹出解释气泡，界面更紧凑
- 设置界面整体放大：窗口 960×720，行标题、导航、下拉框、输入框、开关、按钮和说明气泡的字号全面调大，更易读
- 系统托盘菜单字号由 15px 调大到 17px

## v1.0.2 - 2026-06-11

### 修复
- 修复 v1.0.1 安装版启动即崩溃（`ModuleNotFoundError: No module named 'xml.etree'`）：打包配置遗漏了标准库 `xml.etree`，现已显式打入安装包

## v1.0.1 - 2026-06-11

### 修复
- 修复「检查更新」在共享出口 IP 网络下报 `HTTP Error 403: rate limit exceeded` 的问题：GitHub API 被限流时自动降级到不限流的 releases.atom 订阅源，版本检查、更新说明获取和安装包下载均不再依赖 API 配额

## v1.0.0 - 2026-06-11

### 新功能
- 应用内自动更新：启动后静默检查 GitHub Releases，发现新版本时弹出更新卡片，展示更新说明，一键下载（带进度）、静默安装并自动重启
- 更新完成后首次启动自动展示一次该版本的更新日志
- 设置窗口新增「关于」分区：GitHub 仓库链接和「检查更新」按钮
- 全新应用图标，安装包、可执行文件、系统托盘和窗口统一使用

### 改进
- 发布流程自动把 tag 版本号写入应用（应用内可见准确版本）
