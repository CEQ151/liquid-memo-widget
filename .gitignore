__pycache__/
*.py[cod]
*.log

build/
dist/
*.spec

.env
.venv/
venv/

*.zip
*.7z
*.rar

*.tmp
*.bak
*.bad-*.json

# Personalized surprise-mode source; commit only the generated encrypted payload.
private/
*.surprise.json

# External reference only (algorithm port source); not needed at runtime, kept local.
WebGL-Fluid-Simulation/

# Dev-only scratch under the fluid sandbox (not part of the shipped ink-wash).
LiquidMemoWidget/experimental_fluid/fenxi.txt
LiquidMemoWidget/experimental_fluid/surprise_bg_spike.py

